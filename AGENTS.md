# Agent Instructions

Before starting any development, branching, commit, merge, release, hotfix, refactoring, or cleanup task, read and follow the installed `$git-workflow-manager` skill:

`C:\Users\user\.codex\skills\git-workflow-manager\SKILL.md`

Use that skill as the source of truth for branch selection, `develop/*` integration, refactoring merge strategy, version handling, tags, context switches, and stale branch cleanup.
